# 四阶段流水线 — 流程问题经验积累

> 用途：记录各阶段执行中发现的流程偏差和纠正措施，用于后续整体流程迭代。
> 更新方式：每次用户指出流程问题后追加一条记录。
> 维护人：AI（用户反馈驱动）

---

## 记录列表

| # | 日期 | 阶段 | 问题类型 | 问题摘要 | 纠正措施 |
|:--:|------|------|:---:|---------|---------|
| 1 | 2026-07-03 | Stage 3 | 产出格式错误 | 线框图生成了 `.md` ASCII 伪图，正确格式应为 `.excalidraw`（JSON 结构化矢量图） | 生成线框图前必须确认目标格式为 `.excalidraw`，不可以用 markdown ASCII 替代 |
| 2 | 2026-07-03 | Stage 3 | 跳过确认节点 | 线框图未按 CLAUDE.md 4.4 要求"逐页确认"，一次性输出 8 页 | 严格遵守"每页→确认→下一页"节奏，即使内容多也不批处理 |
| 3 | 2026-07-03 | Stage 3 | 线框图过度绘制 | 折线图/柱状图/环形图等图表用大量 line+rect 元素模拟数据图形，导致绘制成本高且线框图中折线走向混乱不准确 | 线框图中的图表区域无需呈现具体数据图形，仅用标注占位说明”此处为XX图/XX表”即可。图表只画容器框+标题+数据源标注+占位标记（如 [ 图表区 · 占位 ]），不画模拟数据点/柱/弧线 |
| 4 | 2026-07-03 | Stage 4 | 用户输入未二次确认 | 用户提供的 make 项目路径 `jzxznlhxtfxpt` 被 AI 误读为 `jzxznlxthxtfxpt`（h/x 字符颠倒），Test-Path 返回 False 后未向用户确认路径是否正确，而是自行推断为”权限问题”并继续在错误路径操作，错误持续放大 | 接收用户提供的路径/URL/ID 等字符串输入后，若首次校验失败（如 Test-Path False），**必须先向用户确认输入是否正确**，不得跳过确认自行推断原因。长链路操作前（如创建目录结构、安装依赖）也应先向用户确认关键参数，而非默认执行 |
| 5 | 2026-07-03 | Stage 4 | 环境依赖未预检 | make 项目只装了 `antd` 和 `@ant-design/icons`，CLAUDE.md 技术栈声明的 `echarts`/`echarts-for-react`/`react-grid-layout`/`react-markdown`/`remark-gfm` 均未安装，导致 Dashboard.tsx 引用了 echarts-for-react 后 Axhub Make 报 `resource-load` 错误。排查链路长（ultra-minimal→ConfigProvider→Sider+Menu→加回 Dashboard），每次增量添加都要用户测试，浪费大量往返 | **Stage 4 启动时，拿到 make 项目路径后，必须先做环境检查**：① 读 CLAUDE.md 技术栈表 → 列出全部依赖清单；② 逐个 `ls node_modules/<包名>` 验证已安装；③ 缺失的统一 `npm install` 一次性补齐。确认环境就绪后再开始写原型代码。此检查应在”5.1 获取 make 项目路径”步骤中执行 |
| 6 | 2026-07-03 | Stage 4 | 布局未锁定视口 | index.tsx 外层 Layout 用了 `minHeight: '100vh'`，页面内容多时撑高整个 body，导致 Sider 跟着内容区一起滚动。DESIGN.md 定义了固定 Header(56px)+Sider(220px)+Content 的经典布局，但没有把视口高度锁定在 100vh 并让 Content 内部 overflow 滚动 | **布局实现后必须验证滚动行为**：① 外层 Layout 用 `height: '100vh'` 而非 `minHeight`；② 内层 Layout(Sider+Content) 用 `height: calc(100vh - Header高)`；③ Content 设 `overflow: auto` 独立滚动；④ Sider 设 `overflow: auto` + `height: 100%` 防止菜单过长时不可达。侧边栏在任何页面内容高度下都不应跟随内容滚动 |
| 7 | 2026-07-03 | Stage 4 | DESIGN.md 合规未校验 | 每次写完原型代码后，没有拿 DESIGN.md 做逐项对照检查，导致布局缺失 Header 栏、侧边栏滚动等与设计规范的偏离未被及时发现，靠用户逐项指出后才修复 | **每次原型代码输出后，必须对照 DESIGN.md 做一次合规快检**：① 布局结构（Header/Sider/Content 是否齐全、层级是否正确）；② Token 引用（颜色/字体/间距/圆角/阴影是否用了硬编码值而非 DESIGN.md 定义的 token）；③ 组件模式（StatCard/ChartCard 等 DESIGN.md 约定的组件模式是否正确使用）。检查结果附在代码输出说明中 |
| 8 | 2026-07-03 | Stage 4 | 视觉冗余 + 间距过大 | ① 内容区既有面包屑又放了独立页面大标题，信息冗余；② Content padding 16px、Row gutter 12px、Card gap 12px 导致数据看板过于松散，不够紧凑 | **页面标题由面包屑唯一承载**，内容区不重复放置大标题。**分析看板类页面采用紧凑间距**：Content padding 12px、Row gutter [8,8]、Card gap 8px、图表高度按同行卡片对齐（复合卡片内有图表+文字时压缩图表高度使其与同行纯文字卡片齐平）。这些约定写入 DESIGN.md 的 content / page-title / dashboard 节 |
| 9 | 2026-07-03 | Stage 4 | 卡片等高对齐方向搞反 | Row 2 两张卡片"箱量与效率趋势"和"在港船舶进度"底部不齐平，AI 反复压缩左卡内容（图表 360→200、padding→12、字号→20），但问题根源是右卡内容少导致自然高度不够。最后才发现：左卡内容多（图表+统计+tabList），右卡内容少（4条进度），Row/Col 已让两列等高，只需给**内容少的那张卡**加 `height: '100%'` 即可撑满对齐 | **两张卡片在同行不等高时，先判断谁是"短板"**：① 内容多的卡保持自然高度（它决定行高）；② 内容少的卡加 `height: '100%'` 撑满所在 Col 即可。切莫盲目压缩内容多的卡，那只是拆东墙补西墙 |
| 10 | 2026-07-03 | Stage 4 | 筛选栏布局模式 | 筛选栏最初所有控件（Select+DatePicker+今日本周本月+刷新）全挤在一个 `<Space>` 里，没有层次，筛选和操作混在一起 | **看板筛选栏标准模式**：① `flex` + `justifyContent: space-between` 分成左右两区；② 左侧放筛选控件（Select/DatePicker/时间快捷切换）；③ 右侧放操作按钮（刷新/导出等）；④ 时间快捷切换用 `Segmented` 分段控件而非散落的 text Button，视觉上更紧凑专业。此模式应作为后续所有分析看板页面的筛选栏模板 |
| 11 | 2026-07-03 | Stage 4 | 菜单层级冗余 | 菜单组"总览看板"下只有一个子项"理货总览"，用户需先展开再点击，多一步操作。顶部栏同理 | **单子组折叠规则**：若菜单组只有 1 个子页面 → 渲染为平级菜单项，点击组名直接进入页面。多子组保留 SubMenu。面包屑同步适配：单子组不显示冗余二级标题。实现要点：`KEY_TO_PAGE` 映射表处理 menuKey→pageKey 转换，`selectedKeys` 单子组用 groupKey、多子组用 childKey |
| 12 | 2026-07-03 | Stage 4 | 缺少布局切换能力 | 初始只有侧边栏布局，无法满足不同用户偏好（如顶部导航占用更少横向空间） | **布局切换模式**：点击 Header 右侧齿轮 ⚙ 在 `side`（侧边栏）和 `top`（顶部栏）之间切换。① side 模式：Header + Sider(220px inline menu) + Content；② top 模式：Header(水平 menu) + Content(全宽)。搜索框固定右侧图标区，两种模式一致。齿轮图标在 side 模式灰色、top 模式蓝色高亮 |

---

> 版本：v1.0 | 创建日期：2026-07-03
